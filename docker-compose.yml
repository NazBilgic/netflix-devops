services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app
    environment:
      - PORT=5000

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend


